"""Identity module: organizations (the tenant root) and users.

Story 0.4 adds the Organization model + repository. Users, signup/login, and the
api/service/schemas files are populated in Epic 1.
"""
