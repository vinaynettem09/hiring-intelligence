# Technical Debt (TD log)

Intentional shortcuts we've chosen for speed. One line each. Every entry has a
**migration trigger** so a shortcut can't silently become permanent architecture.
When a trigger fires, address it (and remove the entry).

| ID | Debt | Incurred | Reason | Migration trigger |
|---|---|---|---|---|
| TD-001 | Backend container runs as **root** | Story 0.2 | dev speed | Story 10 (prod hardening) — before any real users |
| TD-002 | Backend image is **single-stage**, not distroless/non-root/slim | Story 0.2 | simplicity | Story 10 / first real deploy |
| ~~TD-003~~ | **RESOLVED (Story 8.1) — CI enforces `mypy --strict` on a clean Linux runner.** Because local mypy was WDAC-blocked on Windows and no CI had ever actually run, strict typing had never executed: Story 8.1 surfaced and fixed **53 latent strict violations** (loose ORM→DTO boundary annotations in `evaluations/service.py`/`repository.py`/`models.py`; test-double + generic-arg typing) with **zero** config weakening (only an `ignore_missing_imports` override for the intentionally-optional, uninstalled `anthropic` SDK). Still not in pre-commit (kept fast) — CI is the gate | Story 0.1 | keep commits fast | Fired: CI `backend-quality` now blocks any type regression before merge |
| TD-004 | **Frontend not containerized** (host-only dev) | Story 0.2 | faster reload loop | need a one-command full-stack env / cross-service e2e |
| TD-005 | **`jwt_secret` has a dev default** in Settings | Story 1.2 | zero-config local dev | **Must** set a real secret via env before any deploy (Story 0.9 / first prod) |
| TD-006 | **Access + refresh tokens in `localStorage`** (frontend) | Story 1.2/1.4 | simplest MVP; readable by JS | XSS-exposed (refresh token especially) — move to httpOnly cookies when auth hardens (before real users) |
| TD-007 | **Refresh tokens are not rotated** on use | Story 1.2 | keep 1.2 focused | rotate + revoke-on-reuse when auth hardens; detect token theft |
| TD-008 | **JWT `iss`/`aud` not validated** (only signature + exp) | Story 1.2 | single service today | add issuer/audience claims + validation before prod, once staging/prod/tooling issue tokens (Story 10) |
| TD-009 | **PII minimizer is deterministic regex** (email/URL/phone/self-identifying names/street address + redaction count + fail-closed) — **hardened, NOT solved**: no NER, so names without a lead-in, unusual contact formats, employers/locations, and handles inside code answers can still slip through | Story 5.1 → hardened 5.3 | deterministic/local (no NER dep, no second AI call); errs toward over-redaction; the boundary + fail-closed matter most for MVP egress | Residual risk is documented in `pii.py`. Revisit with NER / a provider-side redaction pass when volume or a real incident justifies it; do **not** treat PII risk as "solved" |
| ~~TD-011~~ | **RESOLVED (Story 5.3).** Evaluation execution held the request DB transaction across the provider call | Story 5.2 | — | Fixed: `EvaluationExecutionService` owns its transaction boundaries via the session factory (read+assemble → release → provider call with no session held → fresh txn to re-check consent + persist). Proven by `test_no_transaction_held_during_provider_call`. |
| TD-010 | **Confidence is a heuristic, not statistically calibrated** — v2 (sufficiency × decisiveness; see resolved TD-016) is more honest than v1's coverage-only, but the weights and the decisiveness curve are still hand-chosen, not fit to outcomes | Story 5.1 (v1) → Story 5.4A (v2) | no hiring-outcome data to calibrate against yet | when hiring-outcome data exists: calibrate confidence against real outcomes and version the algorithm (provenance already records `confidence_algorithm_version`; historical runs retain their original version + number) |
| TD-012 | **Run-number concurrency relies on the DB unique constraint, not app-level locking/retry** | Story 5.2 | serialization per CandidateEvaluation is acceptable for MVP (reviewer-agreed) | if genuine concurrent evaluate/rerun becomes common: add advisory-lock/retry so a racing run resolves gracefully instead of surfacing `EVALUATION_RUN_CONFLICT` (409). The unique constraint already protects the invariant (proven on Postgres) |
| TD-013 | **The one live synthetic quality run is still pending** — the Anthropic SDK/API/model surface is now **verified against current official docs (2026-07-28)** and the SDK is **pinned (`anthropic==0.120.1`, optional extra)**, but a real model call needs an `ANTHROPIC_API_KEY` + outbound network that this build environment does not have | Story 5.3 | CI must make zero real calls; no key here | The founder runs `uv sync --extra anthropic` then `AI_PROVIDER=anthropic ANTHROPIC_API_KEY=… uv run python scripts/run_quality_eval.py` once on the synthetic fixtures and reviews the quality table before merge |
| TD-017 | **Gemini adapter's exact SDK surface is unverified against a live run, and the free tier is data-retaining** — verified from current official docs (2026-07-28: `google-genai`, Interactions API `interactions.create`→`.output_text`, structured `response_format`, model `gemini-3.6-flash`, key `GEMINI_API_KEY`), but token usage is now mapped to the verified Interactions fields (`interaction.usage.total_input_tokens` / `total_output_tokens`, ai.google.dev 2026-07-28 — this is why the first baseline showed `None`), but the exact async wiring, timeout param, and error classes are still parsed **defensively** and unconfirmed live; and the **free tier may train on submitted data** | Story 5.3 | free-tier A-G synthetic evaluation without buying Anthropic credits; CI makes zero real calls | Founder runs the A-G harness once with a real key on SYNTHETIC fixtures + confirms the SDK surface; **never** select `AI_PROVIDER=gemini` for real candidate Evidence while on the free tier. Also: `evaluation_prompt.py` duplicates the Anthropic adapter's render/parse — consolidate the two later |
| ~~TD-016~~ | **RESOLVED (Story 5.4A) — confidence v2.** v1 measured evidence *coverage* only, so strong/weak/mixed evidence alike scored ~1.0 (the first live Gemini A-G run scored A/B/C/G all 1.0). Fixed: `confidence = sufficiency × decisiveness` — coverage is now only the *sufficiency* term, multiplied by a *decisiveness* term that discounts confidence toward a floor as the model's grounded strengths/concerns become evenly split (mixed) or absent (thin). Direction-agnostic and length-blind (proven in `test_confidence.py`). **Residual (folds into TD-010):** (a) the calculator structurally cannot detect a *confidently-wrong* model that fabricates a clean one-sided assessment from thin evidence — that is caught by the model's own ESCALATE behavior + the A-G behavioral gates, not by this number; (b) the decisiveness weighting is a heuristic, not outcome-calibrated | Story 5.3 (surfaced) → Story 5.4A (fixed) | — | Was BLOCKING before the recruiter ConfidenceMeter; the semantic layer is now honest. Calibration against real outcomes remains TD-010 |
| TD-014 | **Cost accounting stores token usage only, not money** | Story 5.3 | pricing changes and is provider/model-specific; baking a monetary cost into immutable rows would rot | derive estimated cost later from a **versioned** pricing table keyed on provider/model (kept out of the Evaluation domain); token counts + exact model provenance are already persisted |
| TD-015 | **No caching of model responses** (deliberate, per reviewer) | Story 5.3 | at MVP volume caching adds invalidation semantics without evidence it's needed; we have fingerprint + idempotency + explicit reruns + immutable history | add content-hash caching keyed on `input_fingerprint` only when measured latency/cost justifies it |
| TD-019 | **ORM models declare foreign keys only in the migrations, not as column-level `ForeignKey`/`relationship`** | Story 8.1 (surfaced) | modules stayed decoupled; the DB enforces referential integrity regardless | SQLAlchemy's unit of work can't dependency-order a multi-table insert, so batch seeds must `flush()` per tier (see `test_evaluation_constraints.py`). This also masked that the **Postgres integration tests had never actually executed** (an `async with pytest.raises(...)` misuse + this insert-ordering bug — both fixed in 8.1, now green in CI). Add column-level `ForeignKey`(+ selective `relationship`) if/when we need ORM navigation or ordered bulk inserts across modules |
| TD-018 | **Decision / timeline APIs hang off the `/evaluations/{id}` resource** (`/evaluations/{cid}/decisions`, `/evaluations/{cid}/timeline`) for UX convenience + routing consistency | Story 6.2/6.3 | the workspace is where these are used, and every route is already keyed by `candidate_evaluation_id` | the true aggregate root is the **CandidateEvaluation** (a decision/timeline exists even when `evaluation_id IS NULL`, which the data model already reflects). Migrate to `/candidate-evaluations/{id}/…` (or an `Application` aggregate) when the API is versioned — an architectural evolution, not an MVP blocker |

<!-- Anticipated (not yet incurred): background jobs will start as FastAPI
BackgroundTasks (Story 0.5). Add a TD then, trigger = need for retries / durable
execution → move to a jobs table + worker, later a real queue. -->
